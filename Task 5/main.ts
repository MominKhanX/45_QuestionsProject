// Store the famous person's name in a variable
let famousPerson: string = "Albert Einstein";

// Compose the message using the stored name
let message: string = `${famousPerson} once said, "Learn from yesterday, live for today, hope for tomorrow. The important thing is not to stop questioning."`;

// Print the message
console.log(message);